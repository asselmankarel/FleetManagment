<UserControl x:Class="Fleetmanagement.Admin.WPF.Views.DriverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fleetmanagement.Admin.WPF.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal" 
             TextOptions.TextRenderingMode="Auto"        
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="1024" d:DesignWidth="1280">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView ItemsSource="{Binding Drivers}"
                  SelectedItem="{Binding SelectedDriver, Mode=TwoWay}" 
                  DisplayMemberPath="FullName" 
                  FontWeight="Bold"
                  ScrollViewer.CanContentScroll="True"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Firstname" Margin="10 20 10 0" FontWeight="Bold"/>
            <TextBox Grid.Row="1" Text="{Binding SelectedDriver.FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10" />

            <Label Grid.Row="2" Content="Lastname" Margin="10 10 10 0" FontWeight="Bold"/>
            <TextBox Grid.Row="3" Text="{Binding SelectedDriver.LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10" />

            <Label Grid.Row="4" Content="National identification number" Margin="10 10 10 0" FontWeight="Bold"/>
            <TextBox Grid.Row="5" Text="{Binding SelectedDriver.NationalIdentificationNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10" />

            <Label Grid.Row="6" Content="Email" Margin="10 10 10 0" FontWeight="Bold"/>
            <TextBox Grid.Row="7" Text="{Binding SelectedDriver.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10" />

            <Label Grid.Row="8" Content="Driverslicense" Margin="10 10 10 0" FontWeight="Bold" />
            <TextBox Grid.Row="9" Text="{Binding SelectedDriver.DriversLicense, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10 0 10 10" />

            <Label Grid.Row="10" Content="Is active" Margin="10 10 10 0" FontWeight="Bold" />
            <CheckBox Grid.Row="11" Margin="10 10 10 0" IsChecked="{Binding SelectedDriver.IsActive}"/>
            
            <Button Grid.Row="12" Content="Save" Width="200" Margin="10" HorizontalAlignment="Left" Foreground="White"/>

        </Grid>
    </Grid>
</UserControl>
